spring.application.name=ctfbackend

# PostgreSQL settings
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/ctf}
spring.datasource.username=ctfuser
spring.datasource.password=ctfpass
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.sql.init.mode=always

# University LDAP
spring.ldap.urls=ldaps://ldap.technikum-wien.at:636
spring.ldap.base=ou=people,dc=technikum-wien,dc=at
# Logging levels and the rolling file appender are governed by logback-spring.xml.
# Override with LOGGING_FILE_NAME in deployments that use a managed log directory.
logging.file.name=${LOGGING_FILE_NAME:./logs/ctf-backend.log}
logging.file.max-size=10MB
logging.file.max-history=7

# File upload settings

spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

challenges.base.path=/opt/ctf/backend/challenges
ctf.ssh.password=${CTF_SSH_PASSWORD}

# Rate Limiting Configuration
rate-limit.enabled=true
rate-limit.global.requests=100
rate-limit.global.duration-seconds=60
rate-limit.login.requests=10
rate-limit.login.duration-seconds=60
rate-limit.flag.requests=30
rate-limit.flag.duration-seconds=60

# admin settings
app.auth.admin-users=if24b120

# Prevent error details in HTTP responses (belt-and-suspenders for Spring's default error page)
server.error.include-message=never
